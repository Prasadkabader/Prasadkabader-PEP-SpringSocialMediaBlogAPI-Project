package com.example.controller;

import org.springframework.web.bind.annotation.RestController;

/**
 * Main controller that combines all other controllers.
 * This is a convenience class to group all API endpoints under a single
 * controller.
 */
@RestController
public class SocialMediaController {
    // This class serves as a marker for the main controller
    // Actual endpoints are defined in AccountController and MessageController
}
